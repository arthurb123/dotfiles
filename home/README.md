# Dotfiles
Custom dotfiles for my Arch Linux setup.

## Usage
1. Clone the repository:
    ```bash
    git clone
    ```
2. Make the install script executable:
    ```bash
    chmod +x install.sh
    ```
3. Run the install script:
    ```bash
    ./install.sh
    ```

### Screenshot
![screenshot](./resources/screenshot.png)